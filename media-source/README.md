# Media source masters

Files in this directory are editing masters and are not copied into
the published `wwwroot` output. Regenerate the responsive pond hero
assets with:

```bash
bash scripts/build-drone-hero.sh
```
