@page "/quote"
@inject LocalizationService Loc

<PageTitle>@Loc.T("quote.title", "Request a Custom Quote - Oxyniti")</PageTitle>

<HeadContent>
    <meta name="robots" content="noindex" />
</HeadContent>

<ComingSoon />

@code {
    protected override void OnInitialized()
    {
        Loc.OnChange += StateHasChanged;
    }
}
